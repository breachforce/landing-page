---
// No frontmatter needed for this component
---

<section class="hero">
  <div class="container hero-content-wrapper">
    <div class="text-content">
      <h1 class="hero-title">
        BreachForce – Mumbai's<br />
        Underground<br />
        Cybersecurity Meetups
      </h1>
      <p class="hero-subtitle">
        A free, grassroots community of hackers, red teamers, defenders, and curious minds.
      </p>
      <a href="https://breachforce.net/upcoming" class="cta-button">
        Join Upcoming Meetup
      </a>
    </div>
    <!-- The image now correctly has no overlay div -->
    <div class="image-content"></div>
  </div>
</section>

<style>
  .hero {
    /* Correct background color from the source */
    background-color: #0f0f10;
    /* This height is crucial for vertical alignment */
    min-height: 0vh;
    display: flex;
    align-items: center;
    padding: 4rem 0;
    width: 100%;
  }

  .hero-content-wrapper {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 3rem;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
  }

  .text-content {
    flex-basis: 55%; /* Give text a bit more space */
    flex-grow: 1;
  }

  .image-content {
    flex-basis: 45%;
    height: 400px;
    position: relative;
    background-image: url('/images/skyline.png');
    background-size: cover;
    background-position: center;
    border-radius: 8px;
  }

  /* The overlay is removed, as it's part of the image itself.
    The .image-overlay class is no longer needed.
  */

  .hero-title {
    /* Using the correct font from Google Fonts */
    font-family: 'Fira Mono', monospace;
    /* Correct font weight from the source */
    font-weight: 500;
    /* Correct color from the source */
    color: #fbf3fa;
    font-size: 45px;
    /* Correct line height from the source */
    line-height: 1.25;
    margin-bottom: 1.5rem;
  }

  .hero-subtitle {
    /* Using the correct font from Google Fonts */
    font-family: 'Fira Sans', sans-serif;
    /* Correct color from the source */
    color: #e0d6de;
    font-size: 18px;
    font-weight: 400;
    line-height: 1.6;
    margin-bottom: 2.5rem;
    max-width: 500px;
  }

  .cta-button {
    display: inline-block;
    /* Correct accent color from the source */
    background-color: #ff6bd8;
    /* Correct button text color from the source */
    color: #000000;
    font-family: 'Fira Sans', sans-serif;
    padding: 1rem 2.5rem;
    font-weight: 600;
    border: none;
    border-radius: 4px;
    transition: background-color 0.3s ease;
    text-decoration: none;
  }

  .cta-button:hover {
    background-color: #f252c5;
  }

  /* Responsive styles for mobile */
  @media (max-width: 768px) {
    .hero {
      min-height: auto;
      text-align: center;
    }
    .hero-content-wrapper {
      flex-direction: column;
    }
    .image-content {
      width: 100%;
      margin-top: 2rem;
      height: 300px;
    }
  }
</style>